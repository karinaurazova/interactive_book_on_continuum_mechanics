name: prototype-ci

on:
  push:
    branches:
      - v0.1-foundation
      - v0.2-platform
      - v0.3-kinematics
      - v0.3.1-hardening
      - v0.4-transport
      - v0.4.1-hardening
      - v0.4.2-progressive-depth
      - v0.5-balance-laws
      - v0.5.1-hardening
      - v0.6-constitutive-modeling
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install
        run: npm install

      - name: Kinematics sanity checks
        run: npm run test:kinematics

      - name: Transport sanity checks
        run: npm run test:transport

      - name: Balance-law sanity checks
        run: npm run test:balance

      - name: Build
        run: npm run build
